import EStylesheet from 'react-native-extended-stylesheet';

export default EStylesheet.create({
  container: {
    flex: 1
  },
  containerBg: {
    position: 'absolute',
    left: 0,
    right: 0,
    top: 0,
    bottom: 0
  }
});
